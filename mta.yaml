_schema-version: "3.2"
ID: compare
description: Generated by Fiori Tools
version: 0.0.3
modules:
- name: compare-destination-content
  type: com.sap.application.content
  requires:
  - name: compare-destination-service
    parameters:
      content-target: true
  - name: compare-repo-host
    parameters:
      service-key:
        name: compare-repo-host-key
  - name: compare-uaa
    parameters:
      service-key:
        name: compare-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: compare_html_repo_host
          ServiceInstanceName: compare-html5-srv
          ServiceKeyName: compare-repo-host-key
          sap.cloud.service: compare
        - Authentication: OAuth2UserTokenExchange
          Name: compare_uaa
          ServiceInstanceName: compare-xsuaa-srv
          ServiceKeyName: compare-uaa-key
          sap.cloud.service: compare
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: compare-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: compare-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - compare.zip
      name: compare
      target-path: resources/
    - artifacts:
      - comepiusecompare.zip
      name: comepiusecompare
      target-path: resources/
- name: compare
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: comepiusecompare
  type: html5
  path: compare
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: compare-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: compare-destination-service
    service-plan: lite
- name: compare-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: compare-xsuaa-srv
    service-plan: application
- name: compare-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: compare-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
